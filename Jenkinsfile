pipeline {
    agent any
    
    stages {
        stage('build') {
            steps {
                sh 'echo "build application.."'
            }
        }

        stage('Run') {
            steps {
                sh 'echo "Running the application..."'
            }
        }

        stage('deploy') {
            steps {
                sh 'echo "deploy the application..."'
            }
        }
    }
}
